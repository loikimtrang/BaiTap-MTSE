<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management Table</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            padding: 2rem;
        }

        .table-container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .table-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .table-header h2 {
            font-size: 1.8rem;
            font-weight: 600;
            margin: 0;
        }

        .btn-add {
            background: rgba(255, 255, 255, 0.2);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
            padding: 0.7rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 600;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            backdrop-filter: blur(10px);
        }

        .btn-add:hover {
            background: rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.5);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 255, 255, 0.2);
        }

        .table-wrapper {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.95rem;
        }

        th {
            background: #f8f9fa;
            color: #495057;
            font-weight: 600;
            padding: 1rem 1.5rem;
            text-align: left;
            border-bottom: 2px solid #e9ecef;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        td {
            padding: 1rem 1.5rem;
            border-bottom: 1px solid #e9ecef;
            color: #495057;
            vertical-align: middle;
        }

        tr:hover {
            background-color: #f8f9fa;
            transition: background-color 0.3s ease;
        }

        tr:nth-child(even) {
            background-color: #fdfdfd;
        }

        tr:nth-child(even):hover {
            background-color: #f8f9fa;
        }

        .email-cell {
            color: #667eea;
            font-weight: 500;
        }

        .name-cell {
            font-weight: 600;
            color: #2c3e50;
        }

        .address-cell {
            color: #6c757d;
            font-size: 0.9rem;
        }

        .action-cell {
            white-space: nowrap;
        }

        .btn {
            display: inline-block;
            padding: 0.4rem 0.8rem;
            margin: 0 0.2rem;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 500;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            text-align: center;
            min-width: 60px;
        }

        .btn-edit {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
        }

        .btn-edit:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(40, 167, 69, 0.3);
        }

        .btn-delete {
            background: linear-gradient(135deg, #dc3545, #e83e8c);
            color: white;
        }

        .btn-delete:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(220, 53, 69, 0.3);
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            color: #6c757d;
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #dee2e6;
        }

        /* Thêm styling cho empty state với nút add */
        .empty-state .btn-add-empty {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 0.8rem 2rem;
            border-radius: 8px;
            text-decoration: none;
            font-size: 1rem;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            margin-top: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .empty-state .btn-add-empty:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        @media (max-width: 768px) {
            body {
                padding: 1rem;
            }
            
            .table-header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }
            
            .table-header h2 {
                font-size: 1.4rem;
            }
            
            .btn-add {
                padding: 0.6rem 1.2rem;
                font-size: 0.85rem;
            }
            
            th, td {
                padding: 0.75rem;
                font-size: 0.9rem;
            }
            
            .btn {
                padding: 0.3rem 0.6rem;
                font-size: 0.8rem;
                margin: 0.1rem;
                display: block;
                width: 100%;
            }
            
            .action-cell {
                min-width: 120px;
            }
        }

        @media (max-width: 480px) {
            .table-wrapper {
                font-size: 0.85rem;
            }
            
            th, td {
                padding: 0.5rem;
            }
            
            .btn-add {
                width: 100%;
                justify-content: center;
            }
        }

        /* Loading animation for better UX */
        .loading {
            display: none;
            text-align: center;
            padding: 2rem;
            color: #6c757d;
        }

        .loading-spinner {
            border: 3px solid #f3f3f3;
            border-top: 3px solid #667eea;
            border-radius: 50%;
            width: 30px;
            height: 30px;
            animation: spin 1s linear infinite;
            margin: 0 auto 1rem;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="table-container">
        <div class="table-header">
            <h2>Quản Lý Người Dùng</h2>
            <a href="/crud" class="btn-add">
                ➕ Thêm người dùng
            </a>
        </div>
        
        <div class="table-wrapper">
            <table>
                <thead>
                    <tr>
                        <th>Email</th>
                        <th>Tên</th>
                        <th>Địa chỉ</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (dataList && dataList.length > 0) { %>
                        <% dataList.forEach(user => { %>
                        <tr>
                            <td class="email-cell"><%= user.email %></td>
                            <td class="name-cell"><%= user.firstName %> <%= user.lastName %></td>
                            <td class="address-cell"><%= user.address || 'Chưa cập nhật' %></td>
                            <td class="action-cell">
                                <a href="/edit-crud?id=<%= user.id %>" class="btn btn-edit">
                                    ✏️ Sửa
                                </a>
                                <a href="/delete-crud?id=<%= user.id %>" class="btn btn-delete" 
                                   onclick="return confirm('Bạn có chắc chắn muốn xóa người dùng này?')">
                                    🗑️ Xóa
                                </a>
                            </td>
                        </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="4" class="empty-state">
                                <div>
                                    <i>📋</i>
                                    <p>Không có dữ liệu người dùng</p>
                                    <small>Vui lòng thêm người dùng mới để bắt đầu</small>
                                    <br>
                                    <a href="/crud" class="btn-add-empty">
                                        ➕ Thêm người dùng đầu tiên
                                    </a>
                                </div>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

        <!-- Loading state (có thể sử dụng khi fetch data) -->
        <div class="loading" id="loadingState">
            <div class="loading-spinner"></div>
            <p>Đang tải dữ liệu...</p>
        </div>
    </div>

    <script>
        // Thêm xác nhận trước khi xóa
        document.addEventListener('DOMContentLoaded', function() {
            const deleteButtons = document.querySelectorAll('.btn-delete');
            deleteButtons.forEach(button => {
                button.addEventListener('click', function(e) {
                    if (!confirm('⚠️ Bạn có chắc chắn muốn xóa người dùng này?\n\nHành động này không thể hoàn tác!')) {
                        e.preventDefault();
                    }
                });
            });
        });
    </script>
</body>
</html>
